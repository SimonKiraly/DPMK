# Railway / Nixpacks build for the MHD Košice backend.
# Railway → service → Settings → Root Directory = "backend".

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/server.js"
